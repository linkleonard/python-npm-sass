machine:
    services:
        - docker

dependencies:
    override:
        - docker info
        - docker build --rm=false -t linkleonard/python-npm-sass:latest .

test:
    override:
        - docker run linkleonard/python-npm-sass:latest /bin/bash tests.sh
